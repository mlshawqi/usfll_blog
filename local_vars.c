#include "minishell.h"


void    assigned_var(t_local_var **tmp, char *input)
{
        int     i;
        t_local_var     *lst;

        lst = *tmp;
        while(lst)
        {
                // if(input[i] == '=')
        }
}